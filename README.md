# Passwords
List of passwords probably used by Swedish people.

The `password.txt` list is derived from [1000 most common passwords](https://github.com/DavidWittman/wpxmlrpcbrute/blob/master/wordlists/1000-most-common-passwords.txt).

The `namn.txt` contains all the names from the Swedish calendar.
